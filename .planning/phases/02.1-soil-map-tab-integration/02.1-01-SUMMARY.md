---
status: complete
phase: 02.1-soil-map-tab-integration
plan: "02.1-01"
wave: 1
completed: 2026-04-30
requirements: []
---

# Plan 02.1-01 Summary

## Outcome

Published the committed BUEK250 GeoJSON fixtures into the app runtime assets and introduced a vector-capable layer contract for the soil tab.

## Changes

- Extended `data-pipeline/sync.py` with a declarative vector-output sync step that copies `data/geojson/buek250-{slug}.geojson` files into `app/public/data/geojson/` based on `sources.yaml`.
- Updated `data-pipeline/README.md` so the sync workflow documents vector GeoJSON publication and regenerated layer-source metadata.
- Regenerated `app/src/data/layer_sources.js`, which now includes the BGR / BUEK250 provenance entry for the soil layer.
- Refined `app/src/data/layers.js` so layers can declare `type: 'raster' | 'vector' | 'placeholder'`, and added the canonical soil `geojsonPathPattern`.

## Verification

- `python data-pipeline/sync.py`
- Confirmed `app/public/data/geojson/` contains the five `buek250-*.geojson` files
- Confirmed `app/src/data/layer_sources.js` includes the `soil` / `buek250` source metadata

## Notes

- The Phase 2 output contract remains unchanged: the synced browser assets are copied from the committed `data/geojson/buek250-{slug}.geojson` fixtures rather than rebuilt in a second format.
