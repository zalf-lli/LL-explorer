---
phase: 02.1-soil-map-tab-integration
plan: "02"
type: execute
wave: 2
depends_on:
  - "02.1-01"
files_modified:
  - app/src/components/LLMap/index.jsx
  - app/src/hooks/useGeoJSON.js
  - app/src/data/layers.js
  - app/src/components/MapLegend.jsx
  - app/src/i18n.js
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - Opening the soil tab fetches only the active LL's `buek250-{slug}.geojson` file instead of preloading all soil data.
    - Soil overlay loading or failure does not blank the entire LL detail page or regress the landuse map behavior.
    - Soil polygons render with deterministic styling that distinguishes water/special areas from ordinary soil polygons.
  artifacts:
    - path: app/src/components/LLMap/index.jsx
      provides: Shared Leaflet map shell capable of raster and vector overlays
      contains: soil
    - path: app/src/hooks/useGeoJSON.js
      provides: Cached GeoJSON fetch path reused by the soil overlay
      contains: fetch
    - path: app/src/i18n.js
      provides: User-facing loading/error copy for the soil map experience
      contains: loadError
  key_links:
    - from: app/src/components/LLMap/index.jsx
      to: app/public/data/geojson/buek250-{slug}.geojson
      via: lazy overlay fetch when the soil tab is active
      pattern: buek250-.+\.geojson
    - from: app/src/components/LLMap/index.jsx
      to: app/src/data/layers.js
      via: runtime layer-type dispatch
      pattern: soil
---

<objective>
Render the new soil vector layer inside the existing LL detail map without blocking the page shell.

Purpose: turn the soil tab from a placeholder into a working BUEK250 map overlay while preserving the current LL boundary map, legend area, and source-attribution UI.
Output: lazy per-LL soil GeoJSON fetches, a vector overlay path in `LLMap`, deterministic polygon styling, and clear overlay-level loading/error states.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-soil-map-tab-integration/02.1-RESEARCH.md
@.planning/phases/02.1-soil-map-tab-integration/02.1-01-PLAN.md
@app/src/components/LLMap/index.jsx
@app/src/hooks/useGeoJSON.js
@app/src/components/MapLegend.jsx
@app/src/data/layers.js
@app/src/i18n.js
</context>

<notes>
- Keep the LL boundary shell, outside-mask, and outline behavior intact.
- Use overlay-level state for soil data; only boundary-shell failures should trigger the full map fallback.
- A stable deterministic palette is enough for this phase; do not block on a full semantic BUEK legend export.
</notes>

<tasks>

<task type="auto">
  <name>Task 1: Add a soil vector overlay path to `LLMap` with truly lazy per-LL fetches</name>
  <files>app/src/components/LLMap/index.jsx, app/src/hooks/useGeoJSON.js, app/src/data/layers.js</files>
  <read_first>.planning/phases/02.1-soil-map-tab-integration/02.1-RESEARCH.md, app/src/components/LLMap/index.jsx, app/src/hooks/useGeoJSON.js, app/src/data/layers.js</read_first>
  <action>Teach `LLMap` to branch by layer type so raster tabs keep using PMTiles while the soil tab mounts a vector overlay component. Resolve the active soil asset URL from the configured path pattern and the current LL slug, and fetch it only when `layer === 'soil'`. Reuse the existing GeoJSON cache/in-flight behavior or add a tiny wrapper if needed, but avoid triggering soil requests for other tabs or prefetching all LL files.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - The soil overlay request is keyed by the active LL slug
    - The map does not fetch soil GeoJSON when the user stays on landuse, climate, or economic tabs
    - Landuse still renders through the existing PMTiles path
    - The layer dispatch remains readable rather than becoming a tangle of soil-specific conditionals
  </acceptance_criteria>
  <done>The soil tab has a dedicated vector data path that remains lazy and LL-specific.</done>
</task>

<task type="auto">
  <name>Task 2: Render deterministic soil styling with overlay-level loading and error states</name>
  <files>app/src/components/LLMap/index.jsx, app/src/components/MapLegend.jsx, app/src/i18n.js, app/src/data/layers.js</files>
  <read_first>app/src/components/LLMap/index.jsx, app/src/components/MapLegend.jsx, app/src/i18n.js, data/geojson/buek250-east-brandenburg.geojson, .planning/phases/02.1-soil-map-tab-integration/02.1-RESEARCH.md</read_first>
  <action>Render the fetched soil GeoJSON as a Leaflet vector overlay using deterministic styling rules. Distinguish water or special-area features (for example those with `BEMERKUNG` and no soil symbol) from ordinary soil polygons, and derive stable colors for soil polygons from `SYM_NR` or an equivalent reproducible key. Surface an explicit soil-specific loading message while the overlay is in flight and a clear soil-specific error state if the fetch fails, but keep the basemap, boundary outline, and page shell usable. Update legend/help text only as much as needed to avoid misleading the user about unavailable data.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - Soil polygons render inside the current LL boundary map for the active soil tab
    - Water/special areas are visually distinguishable from ordinary soil polygons
    - A missing or broken soil file does not blank the whole LL detail view
    - The info control can show the soil/BGR source metadata when the soil tab is active
  </acceptance_criteria>
  <done>The soil map tab ships as a usable, resilient vector overlay instead of a coming-soon placeholder.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python data-pipeline/sync.py` has been run so the app can fetch the synced soil GeoJSON assets
- [ ] `npm run lint` succeeds in `app/`
- [ ] `npm run build` succeeds in `app/`
- [ ] The soil tab fetches `data/geojson/buek250-{slug}.geojson` only for the active LL
- [ ] A soil-asset load failure preserves the LL page shell and shows a clear map-level error state
</verification>

<success_criteria>

- All tasks completed
- The soil tab renders committed BUEK250 vector data for each LL
- Soil loading is lazy and LL-specific
- Existing map behavior for landuse and the surrounding LL detail page remains intact
  </success_criteria>

<output>
After completion, create `.planning/phases/02.1-soil-map-tab-integration/02.1-02-SUMMARY.md`
</output>
