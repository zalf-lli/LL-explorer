---
phase: 02.1-soil-map-tab-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - data-pipeline/sync.py
  - data-pipeline/README.md
  - app/src/data/layers.js
  - app/src/data/layer_sources.js
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - The Phase 2 vector output contract stays unchanged: `data/geojson/buek250-{slug}.geojson` remains the source artifact pattern.
    - Running `python data-pipeline/sync.py` publishes the committed soil GeoJSON files into `app/public/data/geojson/` so the static app can fetch them.
    - The frontend layer registry can describe a vector-backed soil layer without regressing the existing raster landuse layer.
  artifacts:
    - path: data-pipeline/sync.py
      provides: Static-asset publication path from pipeline outputs into `app/public/data/`
      contains: geojson
    - path: app/src/data/layers.js
      provides: Frontend layer contract for raster and vector-backed tabs
      contains: soil
    - path: app/src/data/layer_sources.js
      provides: Generated source-attribution metadata used by the map info control
      contains: buek250
  key_links:
    - from: data-pipeline/sync.py
      to: app/public/data/geojson/buek250-{slug}.geojson
      via: copy step during sync
      pattern: buek250-.+\.geojson
    - from: app/src/data/layers.js
      to: app/src/components/LLMap/index.jsx
      via: layer config consumed at runtime
      pattern: soil
---

<objective>
Prepare the runtime delivery contract for the soil layer before turning the tab on.

Purpose: make the committed BUEK250 GeoJSON available to the browser and give the frontend a clean vector-layer configuration model without breaking the existing map behavior mid-phase.
Output: synced soil GeoJSON assets under `app/public/data/geojson/`, updated sync docs, and a layer registry that can describe a soil vector overlay.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-soil-map-tab-integration/02.1-RESEARCH.md
@data-pipeline/sync.py
@data-pipeline/sources/sources.yaml
@data-pipeline/README.md
@app/src/data/layers.js
@app/src/data/layer_sources.js
</context>

<notes>
- Do not rebuild or rename the Phase 2 vector outputs; consume them as committed fixtures.
- Keep `soil` effectively off in the UI until the actual vector rendering path exists in Plan 02.
- Preserve the current landuse PMTiles behavior exactly.
</notes>

<tasks>

<task type="auto">
  <name>Task 1: Publish the committed soil GeoJSON files through `sync.py`</name>
  <files>data-pipeline/sync.py, data-pipeline/README.md</files>
  <read_first>.planning/phases/02.1-soil-map-tab-integration/02.1-RESEARCH.md, data-pipeline/sync.py, data-pipeline/sources/sources.yaml, data-pipeline/README.md</read_first>
  <action>Extend the sync pipeline so it copies the committed `data/geojson/buek250-{slug}.geojson` files into `app/public/data/geojson/` during the normal `python data-pipeline/sync.py` run. Reuse the declarative information already present in `sources.yaml` rather than hardcoding individual LL slugs in multiple places. Update `data-pipeline/README.md` so operators know that sync now publishes vector GeoJSON assets alongside metadata and PMTiles.</action>
  <verify>python data-pipeline/sync.py</verify>
  <acceptance_criteria>
    - `python data-pipeline/sync.py` copies the BUEK250 GeoJSON fixtures into `app/public/data/geojson/`
    - The sync logic derives the expected output pattern from the existing source registry or a single well-defined helper
    - `data-pipeline/README.md` documents that vector GeoJSON files are now synced into the app runtime assets
  </acceptance_criteria>
  <done>The static app can request the committed soil GeoJSON files after a normal sync run.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the frontend layer registry to describe a vector-backed soil tab</name>
  <files>app/src/data/layers.js, app/src/data/layer_sources.js</files>
  <read_first>app/src/data/layers.js, app/src/data/layer_sources.js, app/src/components/LLMap/index.jsx, .planning/phases/02.1-soil-map-tab-integration/02.1-RESEARCH.md</read_first>
  <action>Refine the layer registry so the app can distinguish raster and vector-backed tabs. Add the minimum metadata needed for later LL-specific soil fetches, such as an explicit layer type and a `geojson` path pattern or equivalent slug-aware contract for the soil layer. Ensure the generated source metadata includes the soil/BGR entry after sync so the existing map info control can show correct attribution once the tab is enabled. Do not flip the soil tab into full runtime rendering yet if that would make the intermediate state fragile.</action>
  <verify>rg -n "soil|geojson|vector|buek250" app/src/data/layers.js app/src/data/layer_sources.js</verify>
  <acceptance_criteria>
    - `layers.js` can express a vector-backed layer without overloading `pmtilesUrl`
    - The soil layer has a single canonical runtime asset pattern keyed by LL slug
    - `layer_sources.js` includes the BUEK250 source metadata after sync regeneration
    - The landuse layer contract remains valid and unchanged in behavior
  </acceptance_criteria>
  <done>The frontend has a clean runtime contract for a soil vector layer and correct provenance metadata.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python data-pipeline/sync.py` succeeds from the repo root
- [ ] `app/public/data/geojson/` contains the five `buek250-{slug}.geojson` files after sync
- [ ] `app/src/data/layer_sources.js` includes a soil/BGR entry generated from `sources.yaml`
- [ ] The landuse layer still builds against the unchanged raster contract
</verification>

<success_criteria>

- All tasks completed
- The static app can fetch soil GeoJSON assets from `app/public/data/`
- The frontend layer registry can describe vector-backed layers cleanly
- Phase 2 output files remain the source contract, not a duplicated fork
  </success_criteria>

<output>
After completion, create `.planning/phases/02.1-soil-map-tab-integration/02.1-01-SUMMARY.md`
</output>
