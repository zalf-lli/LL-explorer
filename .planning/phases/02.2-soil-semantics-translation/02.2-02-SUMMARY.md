---
status: complete
phase: 02.2-soil-semantics-translation
plan: "02.2-02"
wave: 2
completed: 2026-04-30
requirements: []
---

# Plan 02.2-02 Summary

## Outcome

Made the richer soil contract real in the pipeline by rebuilding the BUEK250 fixtures and replacing the old smoke-test expectations with semantic-contract assertions.

## Changes

- Rebuilt all five `data/geojson/buek250-*.geojson` fixtures through the new semantic export path.
- Updated `data-pipeline/tests/test_pipeline_outputs.py` so the test contract now asserts semantic fields, explicit `feature_kind` values, and translated labels instead of the old `soil_name` / `soil_type_*` shape.
- Synced the refreshed vector outputs into `app/public/data/geojson/` via `data-pipeline/sync.py`.

## Verification

- `.\data-pipeline\.venv\Scripts\python.exe data-pipeline\python\build_vector.py --layer buek250`
- `.\data-pipeline\.venv\Scripts\python.exe -m pytest data-pipeline\tests\ -q --tb=short`
- `.\data-pipeline\.venv\Scripts\python.exe data-pipeline\sync.py`

## Notes

- `pytest` passed with a Windows cache-permission warning only; the semantic contract checks themselves all passed.
