---
phase: 02.2-soil-semantics-translation
plan: "03"
type: execute
wave: 3
depends_on:
  - "02.2-02"
files_modified:
  - app/src/components/LLMap/index.jsx
  - app/src/data/layers.js
  - app/src/components/MapLegend.jsx
  - app/src/i18n.js
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - The SQLite database is the authoritative source for soil semantics, but the runtime contract must expose provenance and explicit fallback behavior instead of leaking raw table structure into the UI.
    - English soil labels must come from a reproducible, repo-committed translation or normalization layer rather than manual edits inside emitted GeoJSON fixtures.
    - Special or water areas must remain resilient in the UI: failures or sparse metadata must not blank the LL detail shell or force the app back onto raw-field heuristics.
  artifacts:
    - path: app/src/components/LLMap/index.jsx
      provides: Frontend soil rendering path that consumes semantic fields first
      contains: soil
    - path: app/src/data/layers.js
      provides: Soil legend and layer contract aligned with the richer metadata export
      contains: soil
    - path: app/src/i18n.js
      provides: Bilingual copy for the updated soil legend/info experience
      contains: soil
  key_links:
    - from: app/src/components/LLMap/index.jsx
      to: app/public/data/geojson/buek250-{slug}.geojson
      via: runtime semantic field consumption
      pattern: buek250-.+\.geojson
    - from: app/src/data/layers.js
      to: app/src/components/MapLegend.jsx
      via: semantic soil legend contract
      pattern: soil
---

<objective>
Migrate the frontend soil experience to the new semantic contract.

Purpose: make the app consume the richer soil metadata directly so future legend, info, and chart work builds on stable bilingual semantics instead of raw database leftovers.
Output: soil map rendering and legend/info behavior that prefers semantic fields over direct inspection of `SYM_NR`, `BEMERKUNG`, and `soil_type_*`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-soil-semantics-translation/02.2-RESEARCH.md
@.planning/phases/02.2-soil-semantics-translation/02.2-02-PLAN.md
@app/src/components/LLMap/index.jsx
@app/src/data/layers.js
@app/src/components/MapLegend.jsx
@app/src/i18n.js
</context>

<notes>
- Preserve the lazy-loading and overlay-level resilience added in Phase 2.1.
- The goal is not a full soil explorer yet; it is a trustworthy semantic contract in the existing map UI.
- Keep raw IDs available for debugging, but stop making user-facing behavior depend on brittle null heuristics.
</notes>

<tasks>

<task type="auto">
  <name>Task 1: Update soil rendering to consume semantic fields first</name>
  <files>app/src/components/LLMap/index.jsx, app/src/data/layers.js</files>
  <read_first>.planning/phases/02.2-soil-semantics-translation/02.2-RESEARCH.md, app/src/components/LLMap/index.jsx, app/src/data/layers.js, data/geojson/buek250-east-brandenburg.geojson</read_first>
  <action>Refactor the soil map rendering path so it derives feature type, styling decisions, and any user-facing labels from the new semantic export rather than from direct null checks on `SYM_NR`, `BEMERKUNG`, or the old `soil_type_*` fields. Keep low-level identifiers as a fallback or debugging aid, not the first-class UI contract.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - The frontend reads semantic feature-kind and label fields first when rendering the soil layer
    - Styling and special-area handling remain deterministic and resilient
    - The old weak fields are no longer the main UI decision path
  </acceptance_criteria>
  <done>The map consumes the new semantic contract instead of reverse-engineering meaning from raw fields.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh soil legend and info copy around the richer contract</name>
  <files>app/src/data/layers.js, app/src/components/MapLegend.jsx, app/src/i18n.js</files>
  <read_first>app/src/data/layers.js, app/src/components/MapLegend.jsx, app/src/i18n.js, .planning/phases/02.2-soil-semantics-translation/02.2-RESEARCH.md</read_first>
  <action>Upgrade the soil legend and nearby explanatory copy so the map no longer presents the layer as a generic two-bucket placeholder. Use the richer semantic groupings and bilingual labels where practical, while keeping the UI concise and preserving the existing resilient map shell from Phase 2.1.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - The soil legend and info text reflect the richer semantic export
    - Bilingual user-facing copy exists for the new legend or explanatory labels
    - The LL detail shell remains stable if some semantic values are sparse or special-area-only
  </acceptance_criteria>
  <done>The frontend presents a more trustworthy soil experience that is aligned with the new contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` succeeds in `app/`
- [ ] `npm run build` succeeds in `app/`
- [ ] The soil map reads semantic fields first rather than relying primarily on raw field null checks
- [ ] The updated legend/info copy remains bilingual and does not regress the existing resilient map behavior
</verification>

<success_criteria>

- All tasks completed
- The frontend consumes the improved soil contract directly
- Soil legend/info behavior is richer and more explicit than the current placeholder treatment
- The soil map remains lazy, resilient, and compatible with Phase 2.1 behavior
  </success_criteria>

<output>
After completion, create `.planning/phases/02.2-soil-semantics-translation/02.2-03-SUMMARY.md`
</output>
