---
phase: 02.2-soil-semantics-translation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - data-pipeline/python/soil_semantics.py
  - data-pipeline/python/build_vector.py
  - data-pipeline/sources/sources.yaml
  - data-pipeline/README.md
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - The SQLite database is the authoritative source for soil semantics, but the runtime contract must expose provenance and explicit fallback behavior instead of leaking raw table structure into the UI.
    - English soil labels must come from a reproducible, repo-committed translation or normalization layer rather than manual edits inside emitted GeoJSON fixtures.
    - Special or water areas must become an explicit exported concept so the frontend no longer infers them only from null `SYM_NR` plus `BEMERKUNG`.
  artifacts:
    - path: data-pipeline/python/soil_semantics.py
      provides: Centralized normalization, translation, and semantic extraction helpers for BUEK250
      contains: soil contract
    - path: data-pipeline/python/build_vector.py
      provides: Pipeline integration point that injects the normalized soil contract into emitted GeoJSON
      contains: buek250
    - path: data-pipeline/README.md
      provides: Human-readable contract documentation for the app-facing soil metadata fields
      contains: soil contract
  key_links:
    - from: data-pipeline/sources/sources.yaml
      to: data-pipeline/python/build_vector.py
      via: declarative lookup and semantic configuration
      pattern: buek250
    - from: data-pipeline/python/soil_semantics.py
      to: data/geojson/buek250-{slug}.geojson
      via: exported per-feature semantic fields
      pattern: buek250-.+\.geojson
---

<objective>
Define the new soil metadata contract before widening the export.

Purpose: replace the implicit `soil_name` / `soil_type_*` lookup with a documented semantic layer that captures bilingual labels, grouping, provenance, and special-area handling in one coherent model.
Output: a dedicated soil-semantics helper path, updated source configuration, and README documentation for the app-facing contract.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-soil-semantics-translation/02.2-RESEARCH.md
@data-pipeline/python/build_vector.py
@data-pipeline/sources/sources.yaml
@data-pipeline/README.md
</context>

<notes>
- Keep file-size discipline in mind: the runtime contract should be richer, not a raw dump of all SQLite rows.
- Preserve `GEN_ID`, `SYM_NR`, and `BEMERKUNG` for provenance and debugging, but stop treating them as the primary UI contract.
- Design the contract so future layers or chart exports can reuse the same semantic concepts.
</notes>

<tasks>

<task type="auto">
  <name>Task 1: Extract normalization and translation logic into a dedicated soil-semantics helper</name>
  <files>data-pipeline/python/soil_semantics.py, data-pipeline/python/build_vector.py</files>
  <read_first>.planning/phases/02.2-soil-semantics-translation/02.2-RESEARCH.md, data-pipeline/python/build_vector.py, data-pipeline/sources/sources.yaml</read_first>
  <action>Create a focused helper module that owns the BUEK250 semantic contract: normalization of malformed strings, translation or bilingual-label generation for stable categories, explicit special-area classification, and provenance-aware field assembly. Refactor `build_vector.py` so it consumes this helper instead of embedding the next generation of soil logic inline.</action>
  <verify>rg -n "soil_semantics|feature_kind|provenance|label_en|label_de" data-pipeline/python</verify>
  <acceptance_criteria>
    - The contract-building logic lives in a dedicated helper rather than growing `build_vector.py` into an unstructured lookup script
    - The helper can distinguish ordinary soil units from water or special areas explicitly
    - Translation and cleanup rules are deterministic and repo-tracked
  </acceptance_criteria>
  <done>The pipeline has one clear place where soil semantics are defined, cleaned, and translated.</done>
</task>

<task type="auto">
  <name>Task 2: Document and declare the new app-facing contract</name>
  <files>data-pipeline/sources/sources.yaml, data-pipeline/README.md</files>
  <read_first>data-pipeline/sources/sources.yaml, data-pipeline/README.md, .planning/phases/02.2-soil-semantics-translation/02.2-RESEARCH.md</read_first>
  <action>Extend the BUEK250 source declaration and pipeline documentation so the semantic export is discoverable and stable. Document the new exported field groups, fallback rules for sparse values, and the rule that original German source strings remain traceable even when bilingual display labels are added.</action>
  <verify>rg -n "buek250|semantic|provenance|translation|feature_kind" data-pipeline/sources/sources.yaml data-pipeline/README.md</verify>
  <acceptance_criteria>
    - The repo documents the new soil runtime contract in a place future contributors will actually find
    - Sparse or optional values have an explicit policy instead of leaking through by accident
    - The source declaration reflects the semantic-export path rather than the old raw lookup only
  </acceptance_criteria>
  <done>The new semantic contract is declared and documented before fixture or frontend work begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The semantic extraction logic is centralized in a dedicated helper path
- [ ] `build_vector.py` depends on the helper instead of growing ad hoc translation logic inline
- [ ] `data-pipeline/README.md` documents the new app-facing soil contract and fallback behavior
- [ ] `sources.yaml` reflects the semantic export shape for `buek250`
</verification>

<success_criteria>

- All tasks completed
- The repo has a clear, documented soil semantics contract
- Translation and cleanup rules are reproducible and versioned
- Special areas are modeled intentionally rather than inferred only downstream
  </success_criteria>

<output>
After completion, create `.planning/phases/02.2-soil-semantics-translation/02.2-01-SUMMARY.md`
</output>
