---
phase: 01-ll-content-system
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - app/src/hooks/useLLMetadata.js
  - app/src/pages/Landing.jsx
  - app/src/pages/LLDetail.jsx
  - app/src/components/KPIStrip.jsx
  - app/src/components/Header.jsx
  - app/src/components/LLMap/index.jsx
  - app/src/i18n.js
  - app/src/data/ll_display.js
autonomous: true
requirements:
  - CONTENT-03
  - CONTENT-04
user_setup: []
must_haves:
  truths:
    - The app renders all LL-specific names, taglines, colours, ordering, and placeholder KPI values from `ll_metadata.json` without importing `app/src/data/ll_display.js`.
    - A metadata entry with `"mock": true` shows a bilingual preliminary-data badge on both the landing card and the LL detail header.
    - `npm run build` succeeds after deleting `app/src/data/ll_display.js`.
  artifacts:
    - path: app/src/hooks/useLLMetadata.js
      provides: Frontend normalization layer reading the merged LL metadata contract only
      contains: ll_metadata.json
    - path: app/src/pages/Landing.jsx
      provides: Landing card rendering of LL content and preliminary badge
      contains: mock
    - path: app/src/pages/LLDetail.jsx
      provides: Detail-page header rendering of preliminary badge and metadata-driven display fields
      contains: mock
  key_links:
    - from: app/src/hooks/useLLMetadata.js
      to: app/public/data/ll_metadata.json
      via: runtime fetch and normalization
      pattern: ll_metadata\.json
    - from: app/src/components/LLMap/index.jsx
      to: app/src/hooks/useLLMetadata.js
      via: `ll` prop carrying outline colour from metadata
      pattern: outline
---

<objective>
Migrate the frontend to the merged LL metadata contract and surface preliminary-data state.

Purpose: Eliminate the remaining hardcoded per-LL display config in the app so one metadata file controls content, ordering, colours, and mock-state behavior.
Output: Updated metadata hook and UI consumers, a deleted `app/src/data/ll_display.js`, and visible bilingual preliminary badges on landing and detail views.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ll-content-system/01-RESEARCH.md
@.planning/phases/01-ll-content-system/01-01-SUMMARY.md
@app/src/hooks/useLLMetadata.js
@app/src/pages/Landing.jsx
@app/src/pages/LLDetail.jsx
@app/src/components/KPIStrip.jsx
@app/src/components/Header.jsx
@app/src/components/LLMap/index.jsx
@app/src/i18n.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace `ll_display.js` with metadata-driven normalization</name>
  <files>app/src/hooks/useLLMetadata.js, app/src/components/KPIStrip.jsx, app/src/components/Header.jsx, app/src/components/LLMap/index.jsx, app/src/data/ll_display.js</files>
  <read_first>app/src/hooks/useLLMetadata.js, app/src/data/ll_display.js, app/src/components/KPIStrip.jsx, app/src/components/LLMap/index.jsx, app/public/data/ll_metadata.json</read_first>
  <action>Teach `useLLMetadata` to derive render-ready LL objects entirely from fetched metadata: display order, `num`, `region`, `color`, `colorDark`, `outlineColor`, KPI placeholder values, icon identifier, and content copy. Update downstream consumers to read these fields from `ll` rather than importing `LL_DISPLAY`, `LL_ORDER`, `LL_REGION`, or `getLLOutlineColor`. Once all imports are gone, delete `app/src/data/ll_display.js` and ensure no file references it.</action>
  <verify>rg -n "ll_display" app/src</verify>
  <acceptance_criteria>
    - No `app/src` file imports `../data/ll_display.js` or `../../data/ll_display.js`
    - `KPIStrip.jsx` reads KPI placeholders from the normalized `ll` object
    - `LLMap/index.jsx` reads outline colour from `ll`, not a global lookup function
    - `app/src/data/ll_display.js` is deleted
  </acceptance_criteria>
  <done>The frontend no longer depends on a separate hardcoded LL display module.</done>
</task>

<task type="auto">
  <name>Task 2: Render bilingual preliminary-data badges in landing and detail views</name>
  <files>app/src/pages/Landing.jsx, app/src/pages/LLDetail.jsx, app/src/i18n.js</files>
  <read_first>app/src/pages/Landing.jsx, app/src/pages/LLDetail.jsx, app/src/i18n.js, app/src/theme.js</read_first>
  <action>Add a small shared visual treatment or inline badge markup that appears whenever `ll.mock` is `true`. The label must be bilingual through i18n resources and read `Preliminary data / Vorlaeufige Daten`. Place the badge on each landing card and in the LL detail header without disrupting existing layouts or hiding the tagline.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `app/src/i18n.js` contains translation keys for the preliminary-data badge
    - `Landing.jsx` conditionally renders the badge when `ll.mock` is true
    - `LLDetail.jsx` conditionally renders the badge when `ll.mock` is true
    - The badge text is bilingual and not hardcoded inline in multiple places
  </acceptance_criteria>
  <done>Mock LLs clearly show preliminary-data status on both major user entry points.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` succeeds in `app/`
- [ ] `npm run build` succeeds in `app/`
- [ ] `rg -n "ll_display" app/src` returns no import references
- [ ] At least one `mock: true` LL shows the bilingual badge in both landing and detail UI
</verification>

<success_criteria>

- All tasks completed
- `CONTENT-03` and `CONTENT-04` are satisfied by the UI code
- The frontend reads per-LL display config exclusively from merged metadata
- Preliminary-data state is visible in both required UI surfaces
  </success_criteria>

<output>
After completion, create `.planning/phases/01-ll-content-system/01-02-SUMMARY.md`
</output>
